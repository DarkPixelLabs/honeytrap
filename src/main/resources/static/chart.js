window.renderChart=function(rows){var c=document.getElementById('chart');var x=c.getContext('2d');var w=c.clientWidth||600,h=180;c.width=w;c.height=h;x.clearRect(0,0,w,h);if(!rows.length){x.font='12px monospace';x.fillText('No traffic recorded yet',20,90);return;}var max=1;rows.forEach(function(r){max=Math.max(max,Number(r.hits));});var step=(w-40)/rows.length;rows.forEach(function(r,i){var bh=(Number(r.hits)/max)*(h-40);x.fillRect(20+i*step,h-20-bh,Math.max(2,step-2),bh);});x.font='10px monospace';x.fillText('max '+max,20,12);};
